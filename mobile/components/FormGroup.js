import React from 'react';
import styled from 'styled-components/native';

const FormGroupElement = styled.View`
  margin: 10px 0;
`;

const FormGroup = (props) => <FormGroupElement {...props}/>;

export default FormGroup;
